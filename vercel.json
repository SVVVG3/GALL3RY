{
  "version": 2,
  "framework": "create-react-app",
  "buildCommand": "npm run vercel-build",
  "routes": [
    { 
      "src": "/api/(.*)", 
      "dest": "/api/$1" 
    },
    { 
      "src": "/(.*)", 
      "dest": "/index.html" 
    }
  ]
}
